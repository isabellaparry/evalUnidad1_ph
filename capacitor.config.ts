import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.evaluacionparryisabella',
  appName: 'evaluacionparryisabella',
  webDir: 'www'
};

export default config;
